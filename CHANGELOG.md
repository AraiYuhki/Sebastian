# 変更履歴

本ファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) の書式に従います。
バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に準拠します。

## [未リリース]

### 追加

- 外部スケジューラー連携のガイドとサンプル（`examples/scheduler/`）：常駐サーバーを持たない
  設計のまま、Jenkins の cron トリガー / SCM ポーリング相当を OS 標準機能で実現する構成
  - `poll-and-run.sh`（fetch → 早送りマージ → 実行）、crontab 例、systemd service / timer、
    Windows タスクスケジューラ登録スクリプト、git フック（post-merge / pre-push）
  - README に「外部スケジューラーと組み合わせる（定期実行・push連動）」セクションを追加
- ダッシュボードに「エージェントプール（分散実行）」カードを追加：`agents` の
  URL・トークン（`$NAME` 参照推奨）・能力ラベルを画面から追加・削除できるように
- ダッシュボードに「パラメーター（params）」カードを追加：実行時パラメーターの定義
  （名前・説明・default・choices・必須指定）を画面から追加・編集・削除できるように
  - 「実行」カードに定義済みパラメーターの入力欄（choices はプルダウン）を表示し、
    既定値から変更した値だけを `--param` として渡して実行できる
  - 「承認ゲートを自動承認（--yes）」チェックを追加。approval 付きジョブがある場合は案内を表示
  - 画面からの検証（--validate）は、必須パラメーターへプレースホルダー値を補って構成だけを検査
- ダッシュボードのジョブフォームを新機能に対応：`approval`（承認メッセージ）・`afterScript`（後処理）・
  `reports`（テストレポートのパターン）・`remote` / `labels`（プール実行と能力ラベル）を画面から編集可能に
  - ジョブ一覧にも 🔐 承認あり / 🧪 レポート解析 / 📡 remote などのバッジを表示
- エージェントのラベルルーティング（`labels`）：Jenkins の `agent { label ... }` に相当
  - `agents` の各エントリに能力ラベル（例: `macos` / `xcode` / `gpu`）を宣言し、
    `remote: true` のジョブが `labels` で要求すると、すべて満たすエージェントの中から最も空いているものに割り当て
  - フェイルオーバーも候補内だけで実施。満たすエージェントが存在しない構成は実行前にエラー
- テストレポートの解析（`reports`）：Jenkins の JUnit プラグインに相当
  - JUnit XML 形式のレポートをグロブパターンで収集し、成功／失敗／エラー／スキップ件数と
    失敗テストの一覧（クラス名・テスト名・メッセージ）をコンソールに表示
  - ジョブが失敗した場合もレポートを回収。集計は履歴（`history.json`）にも記録
- 組み込み環境変数：`CI` / `SEBASTIAN_CI` / `SEBASTIAN_CI_PIPELINE` / `SEBASTIAN_CI_JOB` /
  `SEBASTIAN_CI_COMMIT` / `SEBASTIAN_CI_COMMIT_SHORT` / `SEBASTIAN_CI_BRANCH` を全ジョブへ自動注入
  - ユーザーが `env` で同名キーを定義した場合はそちらを優先
- ジョブの後処理（`afterScript`）：Jenkins の `post`（`always`）に相当
  - `script` の成否に関わらず、同じコンテナ（またはシェル）内で最後に実行される
  - 後処理の失敗はジョブの成否に影響しない。ジョブの終了コードは `script` のものを保持
  - コンテナ実行・`shell` 実行・エージェント委譲のすべてに対応
- ダッシュボード（`serve`）のトークン認証：`--token`（または環境変数 `SEBASTIAN_CI_SERVE_TOKEN`）で
  画面とAPI・WebSocketの全リクエストを保護
  - ブラウザは `/?token=<トークン>` で開き、以降は HttpOnly Cookie に引き継がれる
  - API は `Authorization: Bearer` / `X-Sebastian-Token` ヘッダーにも対応。不一致は `401`
  - トークンの比較は一定時間比較（タイミング攻撃対策）。認証なし起動時は警告を表示
- シークレットの自動マスキング：`$NAME` / `${NAME}` でホスト環境変数から展開された値が
  ジョブ出力（コンソール・ログファイル・ダッシュボードのライブログ）に現れた場合、自動で `****` に伏せ字化
  - `env` / `notifications` / `agentToken` など、ホスト参照で解決されたすべての値が対象
  - 誤検知を避けるため4文字未満の値は対象外
- 手動承認ゲート（`approval`）：Jenkins の `input` ステップに相当する実行前の人間による確認
  - ジョブに `approval: <メッセージ>` を書くと、実行直前にコンソールで `y` の入力を待つ
  - 拒否（`y` / `yes` 以外）はジョブ失敗として扱われ、後続ジョブはスキップされる
  - `--yes` で全ゲートを自動承認（無人実行向け）。標準入力が使えない環境では拒否扱い
- 実行時パラメーター（`params`）：Jenkins のパラメータ付きビルドに相当する実行時入力
  - パイプラインに `params` で `default` / `description` / `choices` を定義し、`--param <名前=値>` で値を渡す
  - 解決後の値は環境変数として全ジョブに注入される（優先順位: グローバル `env` < `params` < ジョブ `env`）
  - `default` の無いパラメーターは指定必須。`choices` 範囲外の値・未定義パラメーターへの `--param` は実行前にエラー
  - `--param` 指定時の実行は履歴（スキップ判定）に記録しない（`--job` と同じ扱い）

- ジョブの `shell: true`：コンテナを使わず、ホストマシン上で `script` を直接実行できるように
  - `xcodebuild`（macOS / iOS ビルド）のようにコンテナ化できないツールチェーン向け
  - ホストの環境変数を引き継ぎ、`timeout` / `retry` / `artifacts` などの既存機能はそのまま利用可能。
    タイムアウト・中断時はプロセスツリーごと停止する
  - `agent` / `remote` と組み合わせると、エージェント側のホストで直接実行される
    （Mac の SlaveAgent に Xcode 工程を委譲できる）
  - shell ジョブだけの構成なら podman / docker が無くても実行可能。エージェントも
    エンジンの無いマシンで shell ジョブ専用として起動できるように
- Unity の macOS / iOS 向けビルドのサンプル（`examples/unity-apple.sebastian-ci.yaml`）

## [0.1.0] — 初回リリース

C# (.NET 8) 製のローカル完結型 CI エンジン。以下の機能を備えます。

### コア

- ローカルの Git リポジトリのコミットハッシュを自動取得し、実行済みコミットをスキップ（`--rebuild` で強制再実行）
- Podman / Docker 両対応。各ジョブを `--rm` の使い捨てコンテナで実行し、ワークスペースを `/workspace` にマウント
- 標準出力・標準エラーをリアルタイム表示＋ログファイル保存
- `.sebastian-ci.yaml` の解析・スキーマ検証・正規化
- `needs` とステージによる DAG を構築し、依存のないジョブを並列実行
- 中断（Ctrl+C / SIGTERM / 未処理例外）時に実行中コンテナを確実に停止・削除

### パイプライン機能

- ステージ、マトリックスビルド（`matrix`）、変更検知によるスキップ（`changes`）
- 環境変数の動的インジェクション（`$NAME` でホストの値を注入）
- 失敗制御：`timeout` / `retry` / `continueOnError`
- 同時実行数の上限（`--max-parallel`）、キャッシュ（`cache`）、成果物の保存（`artifacts`）
- 実行履歴・ログ・成果物の永続化（`.sebastian-ci/`）
- リソース監視（メモリ・ディスク）としきい値による警告
- Slack / ChatWork への通知（送信タイミングを `on` で指定）
- 構成検証のみ（`--validate`）、特定ジョブの狙い撃ち実行（`--job`）

### ブラウザ ダッシュボード（`serve`）

- 実行履歴・リソース状況・設定の閲覧
- 設定の編集と検証、ワンクリック実行
- WebSocket による実行ログのライブ表示
- 履歴からジョブ単位の詳細・ログ閲覧

### 分散実行（`agent`）

- リモートエージェントへのジョブ委譲（`agent:` / `remote:`）
- エージェントプールへの自動負荷分散とフェイルオーバー
- トークン認証（`--token` / `agentToken`）
- マスターのソース同期（gzip 圧縮＋コミット差分転送）
- リモート出力の逐次ストリーミング

### 拡張

- プラグイン機構：`plugins` で NuGet パッケージ または ローカルアセンブリを読み込み、
  拡張点の実装を追加できる
  - 通知チャンネル（`INotificationChannel`）：新しい通知先を追加
  - ジョブランナー（`IPluginJobRunner`）：`runner` で選ぶ独自の実行バックエンドを追加
- ダッシュボードの「プラグイン」カードから追加・削除が可能（設定ファイルへ反映し、
  その場で読み込み確認まで検証）。`--validate` もプラグインの解決・読み込みを確認する

### その他

- ゲームエンジン向けサンプル（Godot / Unity / Unreal）
- xUnit ユニットテスト
