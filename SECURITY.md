# セキュリティ

## 脆弱性の報告

セキュリティ上の問題を見つけた場合は、公開の Issue を作成せず、
リポジトリ管理者へ非公開で連絡してください。再現手順と影響範囲を添えていただけると助かります。

## 設計上のセキュリティ配慮

- **秘密情報を設定ファイルに書かない**：トークンや Webhook URL などは `$NAME` 形式で
  ホスト環境変数から受け取ります（`env` / `notifications` / `agentToken` など）。参照先が
  未定義の場合は実行前にエラーで停止し、空のまま処理が進むのを防ぎます。
- **コマンドインジェクション対策**：コンテナ・Git の外部プロセス起動は
  `ProcessStartInfo.ArgumentList` で引数を個別に渡し、シェル経由の引数解釈を避けています。
- **パスの検証**：`artifacts` や `cache` のパスは、リポジトリ外・絶対パス・`..` を
  実行前および実行時に拒否します。
- **分散エージェントの認証**：エージェントは `--token`（または環境変数
  `SEBASTIAN_CI_AGENT_TOKEN`）で保護でき、トークンが一致しないリクエストは 401 で拒否します。

## 運用上の注意

- **エージェントを信頼できないネットワークに公開しない**：エージェントは受け取った
  スクリプトをコンテナで実行します。トークン認証を有効にし、到達範囲を絞ってください。
- **ダッシュボード（`serve`）はローカル利用を想定**しています。設定編集と実行が可能なため、
  信頼できない相手からアクセスできる状態で起動しないでください。
